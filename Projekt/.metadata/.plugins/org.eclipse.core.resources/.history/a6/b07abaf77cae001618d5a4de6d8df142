package Services;

/**
 * The SecretaryPhoneNumberService is the easiest Service
 * If the Client 
 * 
 * @author root
 *
 */
public class SecretaryPhoneNumberService extends Service {
	private String phoneNumber = "1234567890";

	public SecretaryPhoneNumberService(int _id, String _name) {
		super(_id, _name);
	}

	@Override
	public byte[] getAnswer(String payload) {
		String[] payloadArray = this.getPayloadArray(payload);
		String msg = "";
		if (payloadArray.length == 0) {
			msg = phoneNumber;
		} else {
			msg = "Error;Wrong Arguments";
		}
		debugMsg = msg;
		return msg.getBytes();
	}

}
